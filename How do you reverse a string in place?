str1='abcd'
print(str1[::-1])
print(reversed(str1))
print(''.join(list(reversed(str1))))
# Strings are immutable in Python, so reversing a given string in place isn't possible. 
# You'll need to create reversed copies of your target strings to meet the requirement.
print(str1[1::2])
greeting = reversed("Hello, World!")
print(next(greeting))
print(next(greeting))
print(next(greeting))

------------------------------------------------------------------------------------------------------------------------------------------------------------
dcba
<reversed object at 0x7fb25c224310>
dcba
bd
!
d
l


...Program finished with exit code 0
Press ENTER to exit console.

--------------------------------------------------------------------------------------------------------------------------------------------------------------
The strip() method removes any leading, and trailing whitespaces.
synstax--string.strip(characters)

txt = ",,,,,rrttgg.....example....rrr"

x = txt.strip(",.grt")

print(x)//example
